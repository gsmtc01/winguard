node_modules/
dist/
release/
src-tauri/target/
src-tauri/gen/
*.local
.env

# .cargo/config.toml 은 배포 결과물의 정확성에 직결되므로 저장소에 포함한다.
# (CRT 정적 링크가 빠지면 재배포 패키지 없는 PC 에서 실행이 막힌다)
# 머신별 툴체인 경로는 build.bat 이 기본값을 넣고 환경변수로 덮어쓸 수 있다.

# Claude Code 로컬 설정 — 머신별 절대 경로가 들어간다
.claude/settings.local.json

# 모델은 런타임에 HuggingFace 에서 내려받는다 (저장소에 커밋 금지, 파일당 3GB)
models/
*.gguf
*.gguf.part

# 웹사이트 디자인 브리프 — 별도 사이트 저장소에서 관리
WEBSITE_STITCH_BRIEF.md

build_log.txt

# vitest 커버리지 리포트 (생성물)
coverage/
